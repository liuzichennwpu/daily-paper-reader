---
title: "SAGE-Nav: Leveraging LLM Planning and Alignment Fusion for Hierarchical Scene Graph-Guided Navigation"
title_zh: SAGE-Nav：利用LLM规划与对齐融合实现分层场景图引导导航
authors: "Hao Su, Yuehao Huang, Yukai Ma, Yong Liu, Jiajun Lv"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25497v1"
tags: ["query:uav-vln"]
score: 8.0
evidence: 基于LLM的层次化导航，使用语言指令进行物体目标导航，可应用于无人机视觉语言导航
tldr: 针对目标导航任务中现有方法长程推理能力弱、泛化性差的问题，提出SAGE-Nav分层框架。其利用LLM进行全局语义规划，设计分层场景图编码器（HSGE）提取结构感知嵌入，并通过目标感知对齐融合网络（GAFN）动态融合实时感知与先验知识。在i-THOR和RoboTHOR环境中的实验表明，该方法在导航效率和零样本泛化上达到最优，同时保持低控制延迟，适用于物理机器人部署。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25497v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1842, \"height\": 748}, {\"url\": \"assets/figures/arxiv/2606.25497v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 901, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2606.25497v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 901, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2606.25497v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 836, \"height\": 631}, {\"url\": \"assets/figures/arxiv/2606.25497v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 796, \"height\": 809}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25497v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 662}, {\"url\": \"assets/tables/arxiv/2606.25497v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 438}, {\"url\": \"assets/tables/arxiv/2606.25497v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 924, \"height\": 234}, {\"url\": \"assets/tables/arxiv/2606.25497v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 903, \"height\": 406}]"
motivation: 现有ObjNav方法长程推理能力弱，且在新环境中泛化性差。
method: 提出SAGE-Nav，用LLM分解指令为语义航点，HSGE编码场景图，GAFN动态融合感知与拓扑信息。
result: 在i-THOR和RoboTHOR上取得SOTA，显著提升导航效率与零样本泛化。
conclusion: SAGE-Nav实现高效低延迟导航，适合物理机器人部署。
---

## 摘要
目标物体导航（ObjNav）要求具身智能体仅依靠自我中心的视觉观测自主定位指定目标。现有单一方法难以进行长程推理，且泛化到新环境的能力较差。为解决这些局限，我们提出SAGE-Nav，一种融合大语言模型（LLM）推理能力与动态场景图的新型分层框架。关键之处在于，它解耦了异步全局语义规划与高频反应式控制循环。LLM作为全局规划器，将抽象指令分解为一系列语义基元路径点。为将这些规划转化为密集的多模态引导，我们设计了分层场景图编码器（HSGE），利用关系图卷积生成保持语义与空间拓扑的结构感知嵌入。进一步，我们开发了目标感知对齐-融合网络（GAFN），动态融合实时感知与这些结构先验。通过引入具有显式归纳偏置的自适应门控机制，GAFN确保低层级策略具备稳健的视觉-拓扑对齐。在i-THOR和RoboTHOR环境中的广泛评估表明，SAGE-Nav实现了最先进的性能，在导航效率和零样本泛化方面带来显著提升，同时保持了物理机器人部署所需的低控制延迟。

## Abstract
Object-Goal Navigation (ObjNav) requires embodied agents to autonomously locate specified targets using only egocentric visual observations. Existing monolithic methods struggle with long-horizon reasoning and generalize poorly to novel environments. To address these limitations, we propose SAGE-Nav, a novel hierarchical framework that integrates the reasoning capabilities of Large Language Models (LLMs) with dynamic scene graphs. Crucially, it decouples asynchronous global semantic planning from the high-frequency reactive control loop. The LLM serves as a global planner, decomposing abstract instructions into a sequence of semantically grounded waypoints. To translate these plans into dense multi-modal guidance, we design a Hierarchical Scene Graph Encoder (HSGE) that leverages relational graph convolutions to produce structure-aware embeddings preserving both semantic and spatial topology. Furthermore, we develop the Goal-aware Alignment-Fusion Network (GAFN) to dynamically fuse real-time perception with these structural priors. Using an adaptive gating mechanism with an explicit inductive bias, GAFN ensures robust visual-topological alignment for the low-level policy. Extensive evaluations in the i-THOR and RoboTHOR environments demonstrate that SAGE-Nav achieves state-of-the-art performance, delivering substantial gains in navigation efficiency and zero-shot generalization while maintaining the low control latency required for physical robotic deployment.

---

## 论文详细总结（自动生成）

# 论文总结：SAGE-Nav: Leveraging LLM Planning and Alignment Fusion for Hierarchical Scene Graph-Guided Navigation

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：Object-Goal Navigation (ObjNav) 要求智能体仅依靠自我中心视觉观测自主定位指定目标。现有端到端深度强化学习方法虽然能学到直接映射，但存在长程推理能力弱、在新环境中因分布偏移导致性能显著下降的问题。基于场景图的隐式端到端规划缺乏显式层级引导，且嵌入的先验是聚合场景统计量而非结构化、查询相关的规划信号。
- **动机**：受人类空间认知启发（依靠分层规划、语义场景理解和抽象空间推理），作者提出将大语言模型（LLM）的常识推理能力与场景图（SG）的空间结构化表示相结合，实现高层语义推理与低层反应式控制的解耦，从而提升泛化性和长程导航能力。
- **整体含义**：SAGE-Nav 是一种分层导航框架，通过 LLM 驱动的全局规划器生成语义路径点序列，再通过分层场景图编码器（HSGE）和目标感知对齐融合网络（GAFN）将抽象规划转化为多模态结构化引导，最终由 LSTM 策略执行高效、低延迟的导航。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：解耦异步全局语义规划与高频反应式控制。LLM 作为全局规划器（H-GP）进行语义推理，产生可解释的路径点序列；HSGE 将路径点编码为结构感知嵌入；GAFN 动态融合实时视觉感知与结构先验，为低层策略提供稳健的视觉-拓扑对齐信号。
- **关键技术细节**：
  - **LLM驱动的全局规划（H-GP）**：
    - 构建分层场景图 G=(V,E)：对象节点 vi=(Li, pi, fi, li, Di)，Di 为 VLM 摘要；通过混合相似度 Sij=(1-α)S_spatial + αS_semantic 进行增量更新（空间高斯核 + 视觉余弦相似度）。
    - 检索增强子图提取：计算上下文增强语义先验 sv，结合邻域空间提升得到得分 S(v)，取 Top-K 节点形成紧凑子图 GQ。
    - LLM 求解旅行商路径问题（TSPP）：定义分层一致性权重 W(vi,vj)=λ_sem·S_multi-level - λ_dist·D_geo，通过贪心启发式选择下一节点，最终层级下采样得到对象级路径点。
    - 未见目标推理：利用 LLM 内部知识计算语义兼容性先验 φ(q,c)，推断最可能聚类 c*，动态创建虚拟节点加入规划。
  - **分层场景图编码器（HSGE）**：
    - 节点特征 xi = [fi; ψ(pi)]，其中 ψ 为 3D 坐标正弦编码。
    - 每个层级 ℓ 使用 R-GCN（带残差、LayerNorm、ReLU、Dropout）进行关系卷积：h_v^(ℓ)= F_R-GCN^(ℓ)(xv, E^(ℓ)) + W_res xv。
    - 路径点条件注入：对齐权重 αv ∝ exp(xw^T h_v^(ℓ))，然后 ˜h_v^(ℓ) = h_v^(ℓ) + αv xw；跨层级平均池化后通过 MLP 得到路径点嵌入 Ew。
  - **目标感知对齐融合网络（GAFN）**：
    - 结构注入交叉注意力：Fa = CrossAttn(Fv, Ew)，查询为视觉特征 Fv，键值为 Ew。
    - 对齐感知门控融合：计算语义对齐得分 αt=cos(Fv, Ew)，门控向量 λ = σ(MLP([Fv; Fa; Ew; αt]))·(1-ησ(καt))，最终融合特征 Ff = λ ⊙ Fv + (1-λ) ⊙ Fa。该设计引入归纳偏置：随着 αt 增大（感知与路径点对齐），λ 降低，策略从依赖原始感知转向依赖结构上下文。
  - **强化学习优化**：
    - 奖励函数：R_nav = 5·I_vis + 0.01·I_fwd + max(λΔdt,0) - 0.01·(1-I_vis)。
    - 使用 A3C 算法，结合策略梯度、值函数和熵正则化。参考架构：异步 A3C 训练（图 3），多个 worker 收集轨迹，共享优化器更新网络。

## 3. 实验设计
- **数据集/场景**：
  - **iTHOR**：120 个逼真室内场景，4 类房间（厨房、客厅、卧室、浴室），每类前 20 训练、5 验证、5 测试，22 个目标类别。
  - **RoboTHOR**：89 个公寓（约 2.4 倍面积、5.5 倍轨迹长度），60 训练、5 验证、10 测试。
- **Benchmark**：标准 ObjNav 指标：成功率（SR）、路径长度加权成功率（SPL）、距离成功（DTS）。报告所有轨迹（ALL）和最优路径≥5m 的长程场景（L≥5）。
- **对比方法**：随机、SAVN、EOTP、ORG、HOZ、VTNet、L-sTDE、DOA、TDANet、AKGVP、AKGVP-CI、CGI-GAIL、TSOG。此外零样本对比 TargetDriven、HOZ、L-sTDE、AKGVP-CI。效率对比 CogNav、SG-Nav。

## 4. 资源与算力
- 文中明确说明：**所有实验在单个 NVIDIA RTX 3090 GPU 上运行**。训练使用 Adam 优化器，学习率 1e-4，训练 600 万 episodes。HSGE 使用 3 层 R-GCN。未提及训练总时长（小时数），也未说明使用多 GPU 或其他算力细节。

## 5. 实验数量与充分性
- **实验组数**：
  - 主要导航性能实验（表 I）：在 iTHOR 和 RoboTHOR 上对比了 12+ 种基线，覆盖 ALL 和 L≥5 场景。
  - 零样本泛化实验（表 II）：6 个未见类别，对比 4 个基线 + 3 个消融变体。
  - 效率分析（表 III）：对比 CogNav、SG-Nav 的延迟和 LLM 调用次数。
  - 组件消融（表 IV）：7 种配置（逐步加入或去除 H-GP、HSGE、GAFN），验证各模块贡献。
  - 定性结果（图 4、5）：可视化轨迹和失败案例。
- **充分性与公平性**：
  - 实验覆盖了不同环境复杂度（iTHOR 较小、RoboTHOR 更大更复杂），包含长程与零样本场景。
  - 指标全面，基线结果源自原始论文，采用固定随机种子。消融实验逐步分析每模块影响，逻辑清晰。
  - 在效率分析中，公平对比了其他 LLM 方法（CogNav、SG-Nav），确认了延迟优势。
  - **总体充分**，但缺乏实际机器人部署实验（仅在仿真中）、未对比更多 LLM+导航方法（如 SayNav），且缺少跨不同 LLM 后端的鲁棒性实验。

## 6. 主要结论与发现
- SAGE-Nav 在 iTHOR 上达到 **82.47% SR**（ALL）和 **77.22% SR**（L≥5），优于所有对比方法（如 TSOG 80.04%、CGI-GAIL 77.59%）。在 RoboTHOR 上以 **52.35% SR**（ALL）和 **40.82% SR**（L≥5）刷新 SOTA。
- 零样本泛化：**75.05% SR**，远超 AKGVP-CI（69.51%），验证了分层表示捕获可迁移语义先验的能力。
- 效率优势：平均每步延迟 **0.42s**，LLM 调用 **9.1** 次/ episode，显著低于 CogNav（2.20s，15次）和 SG-Nav（0.85s，32次），证明了异步解耦策略的实际有效性。
- 消融表明：GAFN 的对齐融合对零样本性能影响最大（SR 下降 4.4%），LLM 推理和未见目标推理分别贡献约 1.8% 和 1.2% 的 SR 提升。

## 7. 优点
- **方法创新性**：
  - 明确解耦全局 LLM 规划与局部反应式控制，避免频繁调用 LLM，保持低延迟。
  - HSGE 通过 R-GCN 编码分层场景图，保留语义、空间和拓扑层级，路径点条件注入增强了任务相关性。
  - GAFN 使用对齐感知门控（含归纳偏置），使策略在接近目标时自动转向依赖结构上下文，提高了稳定性。
- **实验设计**：
  - 在多个仿真环境（iTHOR、RoboTHOR）上全面评估，包含长程和零样本泛化场景。
  - 效率分析提供了实际部署关心的延迟和计算开销指标。
  - 消融实验逐步验证每个组件的贡献，结论可信。
- **实用性**：单 GPU 实现 SOTA 性能，且控制延迟低，适合物理机器人部署。

## 8. 不足与局限
- **实验局限**：
  - 仅在仿真环境中评估（iTHOR/RoboTHOR），缺乏真实机器人部署验证。
  - 未见真实世界光照、纹理、动态障碍物等挑战。
  - 仅使用了 Qwen2.5-VL-7B 作为 LLM 后端，未对比其他 LLM（如 GPT-4V、LLaMA）的鲁棒性。
- **方法局限**：
  - 依赖离线预构建场景图用于训练，在线构建部分受限于检测器（OpenSEED）和 VLM 质量；文中指出检测错误（如椅子/沙发混淆）是失败原因之一。
  - LLM 规划为贪心搜索（Greedy-TSPP），可能非全局最优，且未见目标推理依赖 LLM 内部先验，在长尾或非常见类别上可能失效。
  - 奖励函数设计简单，未考虑探索奖励或探索-利用权衡的精细调节。
- **偏差风险**：训练集（iTHOR 前 20 场景/类）和测试集（后 5 场景）可能共享结构相似性，但零样本实验（排除 6 类）在一定程度上缓解了过拟合风险。

（完）
