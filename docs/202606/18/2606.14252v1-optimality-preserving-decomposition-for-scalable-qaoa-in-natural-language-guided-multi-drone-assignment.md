---
title: Optimality-Preserving Decomposition for Scalable QAOA in Natural-Language-Guided Multi-Drone Assignment
title_zh: 保持最优性的分解用于自然语言引导的多无人机分配中可扩展的QAOA
authors: "Junyeop Bang, Byongho Lee, Dohyun An, Hwangnam Kim"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14252v1"
tags: ["query:uav-vln"]
score: 9.0
evidence: 通过LLM和量子优化实现自然语言指令驱动的多无人机任务分配
tldr: "多无人机区域分配是NP难问题，经典搜索随规模扩大而失效。本文提出端到端框架，前端用微调LLM将自然语言指令转为QUBO约束，后端用约束保持图分割与压缩合并结合CVaR-QAOA，克服量子硬件限制。理想情况下100%恢复全局最优，真实采样达96.3%。该框架突破经典缩放瓶颈，实现自然语言引导的可扩展任务分配。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14252v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1427, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2606.14252v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1234, \"height\": 680}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14252v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 836, \"height\": 631}, {\"url\": \"assets/tables/arxiv/2606.14252v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 756, \"height\": 259}, {\"url\": \"assets/tables/arxiv/2606.14252v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1059, \"height\": 525}]"
motivation: 多无人机区域分配规模扩大时经典方法因NP难而失效，量子优化受限于硬件，需端到端框架将自然语言映射到量子求解器。
method: 前端用SFT和DPO微调LLM生成鲁棒QUBO约束；后端用约束保持图分割及压缩DP合并，结合CVaR-QAOA与W-state初始化。
result: "理想情况下100%达全局最优，真实QAOA采样达96.3%最优率，实现可扩展的量子任务分配。"
conclusion: 该框架有效结合LLM与量子优化，突破经典缩放瓶颈，为自然语言引导的多无人机分配提供可扩展方案。
---

## 摘要
随着多无人机机队规模扩大，区域分配迅速演变为一个难以处理的NP-hard组合优化问题，使经典穷举搜索不堪重负。虽然量子优化有望打破这些经典瓶颈，但从人类意图到受限量子硬件的复杂空间任务映射仍然是一个严峻挑战。为弥合这一差距，我们提出一个端到端框架，将微调的大型语言模型（LLM）前端与高度可扩展的特定领域量子-经典后端相结合。前端利用监督微调（SFT）和直接偏好优化（DPO），将自由形式的自然语言指令转化为结构稳健的二次无约束二元优化（QUBO）约束，且无假阴性。为克服近期量子设备严格的量子比特限制，我们的框架采用了新颖的约束保持图分割器和基于压缩分隔符的动态规划（DP）合并。通过W态初始化和XY混合器在条件风险价值量子近似优化（CVaR-QAOA）中结构化编码约束，整个流程保持高度紧凑。实验结果表明，该架构规避了经典扩展障碍，在100%的理想化预言机案例和96.3%的实际QAOA采样中恢复全局最优，使得自然语言引导的任务分配在以前难以处理的规模上成为可能。

## Abstract
As multi-drone fleets scale, zone assignment rapidly evolves into an intractable NP-hard combinatorial problem that overwhelms classical exhaustive search. While quantum optimization promises to shatter these classical bottlenecks, mapping complex spatial tasks from human intent to restricted quantum hardware remains a severe challenge. To bridge this gap, we present an end-to-end framework integrating a fine-tuned Large Language Model (LLM) front-end with a highly scalable, domain-specific quantum-classical backend. The front-end utilizes Supervised Fine-Tuning (SFT) and Direct Preference Optimization (DPO) to translate free-form natural language instructions into structurally robust Quadratic Unconstrained Binary Optimization (QUBO) constraints without false negatives. To overcome the strict qubit limits of near-term quantum devices, our framework features a novel constraint-preserving graph partitioner and a compressed separator-based dynamic programming (DP) merge. By structurally encoding constraints via W-state initialization and XY-mixers in Conditional Value-at-Risk Quantum Approximate Optimization (CVaR-QAOA), the pipeline stays highly compact. Empirical results demonstrate that this architecture circumvents classical scaling walls, recovering the global optimum on 100% of idealized oracle cases and 96.3% under real QAOA sampling, enabling natural-language-guided task allocation at previously intractable scales.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

本文聚焦于多无人机（multi-drone）机队在复杂空间任务（如区域监视、灾害响应、物流）中的区域分配问题。随着机队规模扩大，该问题迅速演变为NP-hard的组合优化问题，经典穷举搜索因指数级复杂度（O(M^N)）而在实时场景中不可行。量子优化（如QAOA）有潜力打破经典瓶颈，但现实NISQ（Noisy Intermediate-Scale Quantum）设备受限于严格的量子比特预算和门误差率，无法直接处理大规模问题。此外，将人类自然语言指令转化为可执行的数学优化模型也是一个关键挑战，现有方法容易遗漏约束（假阴性）。本文旨在构建一个端到端框架，将自然语言理解（前端LLM）与可扩展的量子-经典混合后端相结合，实现从人类意图到全局最优解的高效、可扩展映射。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **前端**：利用微调的大型语言模型（LLM，基于Qwen1.5-1.8B + LoRA）将自然语言指令转化为结构稳健的QUBO（Quadratic Unconstrained Binary Optimization）约束，通过监督微调（SFT）和直接偏好优化（DPO）确保无假阴性（false negatives）。
- **后端**：针对量子硬件限制，提出约束保持图分割（Constraint-Preserving Graph Partitioning）+ 压缩基于分离器的动态规划合并（Compressed Separator-DP Merge），将大规模QUBO分解为可适配量子比特预算的子问题，并通过W态初始化和XY混合器在CVaR-QAOA中结构化编码硬约束，避免传统惩罚项带来的量子比特开销。

### 关键技术细节
1. **自然语言约束提取**：LLM（SFT+DPO）输出结构化JSON格式的约束（强制/禁止分配、最小覆盖率等），确定性层验证后生成全局QUBO公式。
2. **约束保持图分割**：使用PyMETIS对无人机-区域二分图进行分割，并在分割前进行“覆盖率候选剪枝”：排除那些绕路到区域超过覆盖率惩罚阈值（λL/2）的无人机-区域对，从而保证全局最优解不被切断。子问题大小严格不超过最大量子比特预算qmax=24。
3. **子QUBO编码与CVaR-QAOA**：
   - 强制/禁止约束通过变量固定直接消除对应决策量子比特。
   - 每个无人机的一组决策量子比特被结构化为哈密顿权重为1的块：用W态初始化（|W⟩ = (1/√k)(|10…0⟩+|01…0⟩+…+|0…01⟩)）和XY混合器（∑(X_a X_b + Y_a Y_b)）保持块内哈密顿权重始终为1，确保量子演化仅在可行的一热子空间内进行，改善收敛景观。
   - 对需要多架无人机的区域（mj≥2）采用精确的二元松弛扩展，而单无人机覆盖委托给经典合并层以节省量子比特。
4. **压缩分离器DP合并**：从左到右的动态规划（DP）基于分离器进行，通过将区域计数严格钳制在其目标占用率mj处，极大缩小DP状态空间；仅保留每个钳制边界状态中累积旅行成本最小的部分分配，利用可分离性和充分性保证全局最优的恢复。

## 3. 实验设计

### 数据集/场景
- **300个场景**，涵盖10种舰队配置（N×M），从7×5到12×4，地图大小为50×50。
- 每个场景包含随机强制/禁止约束（≤3个）、约20%的无人机有运动偏好（αmove=3，其余为1）、约25%的区域有最小请求，全局覆盖率严格强制执行。
- 约束提取评估：单独构建了包含720个黄金约束（gold constraints）的基准，分为三个难度等级（C1单句、C2中等、C345多句/复杂），并与三种基于规则的自然语言处理提取器（spaCy、SRL、OpenIE）以及SFT-only消融进行对比。

### Benchmark
- **理想化预测试**：用穷举子问题枚举器替换QAOA求解器，测试分割与合并层的正确性。
- **真实CVaR-QAOA**：完整管道运行，使用Qiskit-Aer无噪声态向量模拟。

### 对比方法
- 前端提取对比：三种规则提取器、SFT-only，以及本文SFT+DPO。
- 扩展性对比：与经典穷举搜索的运行时进行对比（实测至12×5，更大规模基于O(M^N)外推）。

## 4. 资源与算力

- **GPU**：单块NVIDIA RTX 4080。
- **模拟**：使用Qiskit-Aer进行无噪声态向量模拟（不涉及真实量子硬件）。
- **训练时长**：未明确给出LLM微调（SFT+DPO）的具体训练时间。
- **优化配置**：CVaR-QAOA（p=6，α=0.1）采用COBYLA优化器，8次重新启动（1次INTERP初始化，7次随机）；采样次数和迭代预算根据子QUBO大小线性缩放。

## 5. 实验数量与充分性

- **约束提取实验**：720个黄金约束，3种规则基线 + 2种LLM变体（SFT-only、SFT+DPO），统计精确率、召回率、F1和遗漏数（FN）。实验全面，且SFT+DPO达到极低遗漏（2/720），充分验证了前端可靠性。
- **全局最优性实验**：300个场景，分别用理想化预测试和真实CVaR-QAOA测试匹配率和平均差距。理想化预测试100%匹配，真实采样96.3%匹配，平均最优差距0.04%。实验覆盖10种配置各30个场景，统计量充分。
- **扩展性实验**：扩展到20×5配置，对比中位数运行时，观察交叉点。实验设置合理，展示了经典与量子-经典混合管道的运行时趋势。
- **消融实验**：仅对LLM微调做了SFT vs SFT+DPO消融；对于后端的编码、分割、合并等组件未单独做消融实验（如替换为普通惩罚项、不使用剪枝等）。整体实验设计较为充分，但在分解方法各环节的分解消融方面略显不足。

## 6. 主要结论与发现

1. **前端约束提取**：SFT+DPO几乎消除了假阴性（仅遗漏2/720），召回率达99.7%，F1为0.990，远优于规则方法和SFT-only。
2. **全局最优性**：基于理想化预言机的分割合并层100%恢复全局最优；真实CVaR-QAOA采样时，96.3%场景达到精确全局最优，平均最优差距仅0.04%。所有次优情况均可通过重新运行QAOA恢复，反映的是采样随机性而非结构性缺陷。
3. **可扩展性**：经典穷举搜索运行时指数增长，而所提框架的运行时由子QUBO的QAOA主导（受限于恒定量子比特预算），加上多项式合并开销，呈亚线性增长。并行版本（每个子问题一个GPU流）进一步加速，在状态空间约5×10^7处与经典搜索交叉，此后优势扩大数个数量级。
4. **有效准确率**：引入时间归一化有效准确率（找到全局最优的概率/运行时间），在小规模场景经典搜索占优，但超过交叉点后，本文框架的有效准确率高出数个数量级，证明“牺牲3.7%概率换取指数级加速”在实践中高度有利。

## 7. 优点

- **创新性**：首次将自然语言引导与量子优化结合，实现端到端的无人机任务分配，且后端设计了领域特定的约束编码（W态+XY混合器）和最优保持的分割合并策略，均具有理论保证。
- **完整性**：覆盖从语言输入到量子输出全流程，并验证了前端、后端核心组件；提供了理想化分析（预测试）和真实采样两种评估，严谨性高。
- **可扩展性实验**：不仅展示了当前规模（7×5~12×4），还通过外推展示了更大规模（15×5, 18×5, 20×5）的扩展性，直观显示突破经典瓶颈。
- **公平对比**：前端对比了多种公认的规则基线（spaCy、SRL、OpenIE）和SFT-only消融；后端与全局穷举最优进行了完全对齐的对比，非仅对比启发式方法。

## 8. 不足与局限

- **真实NISQ部署缺失**：所有实验基于无噪声模拟，未在真实量子硬件上运行。作者也承认门噪声和采样方差会影响子问题最优性，需要结合误差缓解和自适应采样策略。
- **分解权衡**：分割大小与分离器状态复杂性之间存在权衡：为适应极紧的量子比特预算而缩减分区会增大边界状态，可能增加经典DP合并的瓶颈。文中未定量分析此权衡对总运行时间的影响。
- **实验覆盖**：仅在10种固定无人机/区域数量配置上测试（7×5到12×4，以及扩展到20×5但仅测运行时），未涉及更大变化（如无人机数远大于区域数、非均匀分布、动态约束等）对算法鲁棒性的影响。消融实验不够完整，未量化W态+XY混合器相对于标准惩罚项编码的改进幅度。
- **适用性限制**：框架目前假设所有无人机同构、区域为二维坐标、任务为分配至固定区域，未考虑连续运动、障碍物、通信延迟等现实因素；语言指令类型也限于简单约束（强制、禁止、最小覆盖、权重），更复杂指令（如时序、优先级）可能无法处理。
- **资源需求**：尽管QUBO分解降低了量子比特需求，但每个子问题仍需多次QAOA优化（多重新启动、迭代），并且经典DP合并规模随分离器大小增长，总计算时间仍可能在极端问题规模下（如大量的约束导致分离器很大）显著增加。

（完）
