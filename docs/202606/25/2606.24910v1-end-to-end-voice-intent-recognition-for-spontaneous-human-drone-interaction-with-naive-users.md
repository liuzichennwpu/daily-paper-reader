---
title: End-to-End Voice Intent Recognition for Spontaneous Human-Drone Interaction with Naive Users
title_zh: 面向非专业用户自然语音人机交互的端到端语音意图识别
authors: "Allan Henry, Solange Rossato, Christian Graff, Sylvain Huet, Jose-Ernesto Gomez-Balderas"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.24910v1"
tags: ["query:uav-vln"]
score: 8.0
evidence: 通过语音意图识别实现语言引导的无人机导航
tldr: "现有无人机语音控制多依赖刚性命令集，无法处理非专业用户在真实任务中的非流畅自发语音。本文提出端到端口语理解架构，采用冻结的自监督学习声学编码器结合轻量LSTM分类头，并通过跨模态知识蒸馏对齐声学与语义表示。在包含29组非专业双人协同的VoiceStick法语语料上，简单命令识别准确率达93%，推理仅需7毫秒，较级联基线快29倍；自发语音准确率达82%，蒸馏机制持续提升鲁棒性。结果表明端到端架构在自发语音无人机遥操作中兼具语义鲁棒、低延迟与校准置信度优势。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24910v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24910v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 776, \"height\": 329, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24910v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 817, \"label\": \"Table\"}]"
motivation: 现有系统难以处理非专业用户的非流畅自发语音，需构建鲁棒低延迟的端到端架构。
method: 冻结SSL声学编码器+LSTM分类头，通过跨模态知识蒸馏对齐声学与语义嵌入。
result: "简单命令93%准确率/7ms延迟（29倍加速），自发语音82%准确率，蒸馏提升鲁棒性。"
conclusion: 端到端架构在自发语音无人机遥操作中更优，平衡语义鲁棒、低延迟与置信度校准。
---

## 摘要
语音控制为手动无人机驾驶提供了一种直观的替代方案，但现有大多数系统依赖于严格的指令词汇表，无法处理非专业用户的自发性、不流畅语音。本文针对这一差距，提出了一种用于法语实时人机交互的端到端口语理解架构。我们的模型将冻结的自监督学习声学编码器与轻量级基于LSTM的分类头相结合，并通过跨模态知识蒸馏目标增强，该目标将声学表示与来自文本教师的语义嵌入对齐，且推理时无需转录。我们在VoiceStick上评估了我们的方法，这是一个新的法语自发性语音语料库，在29对非专家双人组的真实远程操作会话中收集。在简单语音指令上，我们的最佳配置在7毫秒推理延迟下达到93%的准确率，优于级联基线（79%，202毫秒），加速29倍。在完整自发性语音测试集上，我们的架构达到82%的准确率，跨模态蒸馏在所有配置中持续提高了鲁棒性。这些结果表明，端到端架构不仅可行，而且对于自发性语音引导的无人机远程操作更优，兼具语义鲁棒性、低延迟和校准的置信度。

## Abstract
Voice control offers an intuitive alternative to manual drone piloting, yet most existing systems rely on rigid command vocabularies that fail to handle the spontaneous, disfluent speech of naive users. This paper addresses this gap by proposing an End-to-End Spoken Language Understanding architecture for real-time human-drone interaction in French. Our model combines a frozen Self-Supervised Learning acoustic encoder with a lightweight LSTM-based classification head, augmented by a cross-modal knowledge distillation objective that aligns acoustic representations with semantic embeddings from a text teacher, without requiring transcription at inference time. We evaluate our approach on VoiceStick, a novel French corpus of spontaneous speech collected during real teleoperation sessions with 29 nonexpert dyads. On simple voice commands, our best configuration achieves 93% accuracy at 7 ms inference latency, outperforming cascade baselines (79%, 202 ms) with a 29x speedup. On the full spontaneous speech test set, our architecture reaches 82% accuracy, with crossmodal distillation consistently improving robustness across all configurations. These results demonstrate that End-to-End architectures are not only feasible but preferable for spontaneous voice-guided UAV teleoperation, combining semantic robustness, low latency, and calibrated confidence.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有无人机语音控制系统大多依赖严格的、预定义的指令词汇表（如“起飞”“前进”），无法处理非专业用户在真实遥操作场景中产生的**自发、非流畅语音**（例如包含犹豫、重复、修正、口误等）。这严重限制了交互的自然性和用户体验。
- **整体意义**：作者认为，要使无人机操控真正对新手用户直观，必须走向**自然语言和自发语音**。为此，他们提出了一种**端到端口语理解架构**，直接由音频映射到意图，避免传统级联系统（ASR+语义分析）的延迟累积和错误传播问题，满足实时无人机控制的严苛延迟要求（<100ms）。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：结合**冻结的自监督学习（SSL）声学编码器**与**轻量级LSTM分类头**，并通过**跨模态知识蒸馏**在训练时用文本教师（冻结的Sentence Transformer）将声学表示与语义嵌入对齐，但推理时无需转录文本。
- **关键技术细节**：
  - **声学编码器**：基于Wav2Vec 2.0/HuBERT系列预训练模型（多层Transformer），冻结全部参数。对最后N=4层隐藏状态进行**可学习的加权求和**以捕获丰富特征。
  - **时序建模**：使用**LSTM**对帧级特征序列进行长程依赖建模，再通过**注意力池化**（Attentive Pooling）聚合为段级表示`haudio`。
  - **双头结构**：
    - **分类头**：线性层，输出意图概率分布`ŷ`，使用交叉熵损失`L_CE`。
    - **蒸馏头**：将`haudio`投影为语义向量`zaudio`，与文本教师（Sentence Transformer）编码的对应转录文本嵌入`ztext`计算余弦距离`L_cos`。
  - **联合损失**：`L_total = L_CE + L_cos`。推理时仅保留分类头。
- **算法流程**（文字说明）：
  1. 输入原始音频 → 冻结的SSL编码器 → 隐藏状态加权求和 → LSTM → 注意力池化 → 得到共享表示`haudio`。
  2. 训练阶段：`haudio`同时输入分类头（预测意图）和蒸馏头（计算与文本嵌入的余弦距离），优化联合损失。
  3. 推理阶段：仅用分类头输出意图，无需任何文本转录。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：**VoiceStick**（新构建的法语自发语料库）
  - 收集自29对非专业学生双人组（46女12男，平均年龄20.1岁），无无人机经验。
  - 不对称任务：导购（Guide）通过虚拟环境为飞行员（Pilot）提供语音指令，飞行员通过FPV摄像头操控无人机到达3D目标。
  - 包含约2小时音频，4219条话语，2421个唯一句子，669个唯一词。
  - 测试集分为：
    - **Explicit子集**（250条）：明确、无歧义的命令，手动转录作为真实标签。
    - **Full测试集**（412条）：包含自发话语，地面真值经人工重新标注（修正飞行员执行错误、左右反转、并行指令），以消除标注噪声。
- **基准（Baseline）**：
  - **文本驱动Oracle**：输入手动转录文本，使用CamemBERT等模型进行意图分类（上限）。
  - **级联系统（Cascade）**：Whisper Large-v2 ASR + CamemBERT意图分类。
- **对比方法**：多种SSL声学骨干（法语专用XLSR-53-FR、多语言XLSR-53、WavLM、HuBERT等）及有无蒸馏的消融变体。

## 4. 资源与算力
- **显式说明**：所有模型使用**单张RTX 2000 Ada GPU**进行微调。
- **超参数**：AdamW优化器，学习率1e-3，权重衰减1e-4，批次大小1，早停策略（patience=5）。
- **未明确说明**：训练时长、模型参数量、蒸馏教师模型的训练代价等细节未给出。

## 5. 实验数量与充分性
- **实验组数**：
  - 测试了**多种SSL骨干**（至少5种：XLSR-53-FR、XLSR-53、WavLM、HuBERT等）。
  - 对比了**不同文本教师**（CamemBERT-Large、多语言嵌入模型、MiniLM等）。
  - 消融实验：**去掉蒸馏损失**（表I底部），对比有无蒸馏的性能。
  - 评估了**显式子集和全测试集**的准确率，并报告了延迟。
- **充分性评价**：
  - **优点**：覆盖了多种骨干、蒸馏消融、不同教师、延迟指标，统计显著性检验（McNemar检验、Mann-Whitney U检验），置信度分布分析，定性错误分析。实验设计较全面。
  - **不足**：仅法国单一语言，未在英语或其他语言上验证；未与LLM-based系统（如GPT-4）进行直接对比；未进行跨数据集泛化测试（除VoiceStick外无其他数据集）。

## 6. 论文的主要结论与发现
- **端到端架构优于级联系统**：最佳配置（XLSR-53-FR）在显式命令上准确率93%（vs 级联79%），延迟7ms（vs 202ms），加速**29倍**，满足实时控制要求（<100ms）。
- **跨模态蒸馏持续提升鲁棒性**：去掉蒸馏后平均准确率下降7个百分点（从87%降至80%），法语专用模型下降最显著（-11pp）。
- **语言特异性预训练至关重要**：法语专用XLSR-53-FR显著优于通用多语言XLSR-53（93% vs 68%），表明SSL编码器需匹配目标语言。
- **置信度可提供拒绝机制**：正确预测的置信度中位数0.81，错误预测为0.46，72.7%的错误实例低于特定阈值，可用于自动拒识。
- **地面真值噪声挑战**：即使使用完美转录的文本Oracle，全测试集准确率仅92%（显式）和80%（全），反映自发语音意图识别的固有难度。

## 7. 优点：方法或实验设计上的亮点
- **数据真实性**：VoiceStick语料聚焦真实遥操作中的自发语音，包含犹豫、修正等，远超传统读语音命令集。
- **架构巧妙**：冻结SSL编码器+轻量LSTM+注意力池化，兼顾低延迟与鲁棒性；蒸馏目标在训练时借用文本知识但推理零开销，避免ASR误差传播。
- **实验严谨性**：
  - 对地面真值进行人工重新标注（排除飞行员执行错误、左右反转、并行指令），避免评价偏误。
  - 使用统计检验（McNemar、Mann-Whitney）验证性能差异显著性。
  - 提供了置信度分析与错误分类案例，增强解释性。
- **可复现性**：数据集已公开（Zenodo），模型架构清晰。

## 8. 不足与局限
- **实验覆盖有限**：仅验证法语；未评估音频中的环境噪声、远场、多人对话；未与其他端到端方案（如直接微调预训练模型、Transformer分类器）充分比较。
- **任务简化**：仅识别**方向意图**（7类），未考虑**幅度/程度**（如“稍微左转”vs“大转向”），亦未支持组合命令（如“先上升再右转”），作者承认这是未来方向。
- **算力与资源细节缺失**：训练时长、模型参数量、教师模型训练成本等未报告，影响可复现性。
- **数据集规模较小**：仅2小时语音、4219条话语，可能不足以覆盖所有自发语音变体，分布不平衡（“前进”类占33%）。
- **地面真值噪声处理仍不完全**：即使人工重新标注，部分自发语音意图本身存在模糊性（如左右反转在3D环境中可能都正确），准确率上限约80-92%，说明评价指标本身有一定局限性。

（完）
