---
title: Guided Diffusion with Distilled Vision-Language Reliability for Aerial Navigation
title_zh: 基于蒸馏视觉语言可靠性的引导扩散用于空中导航
authors: "Ivan Valuev, Iana Zhura, Valerii Serpiva, Didar Seyidov, Dzmitry Tsetserukou"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13883v1"
tags: ["query:uav-vln"]
score: 9.0
evidence: 无人机空中导航结合视觉语言可靠性
tldr: "无人机自主导航的端到端生成模型在退化感知区域（如玻璃、镜面）易产生危险轨迹。本文提出基于扩散模型的可靠性感知规划器，通过轻量网络蒸馏视觉-语言模型生成场景级可靠性热图，并引入可微分两阶段ESDF代价融合物理和虚拟障碍。在仿真和真实四旋翼上，该方法将障碍侵犯率从40.3%降至9.6%，平均可靠性从0.588提升至0.925，消融实验证实可靠性项是决定性因素。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13883v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1230, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13883v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 543, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13883v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 455, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13883v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1372, \"height\": 513, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13883v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 470, \"label\": \"Table\"}]"
motivation: 现有端到端生成模型无法识别不可靠观测，在玻璃、镜面等退化区域产生危险轨迹。
method: 设计可靠性感知扩散规划器，用蒸馏自VLM的轻量网络输出热图，结合可微分两阶段ESDF代价融合物理和虚拟障碍。
result: "障碍侵犯率从40.3%降至9.6%，平均可靠性从0.588升至0.925，蒸馏使速度提升2倍。"
conclusion: 可靠性热图显著提升导航安全性，蒸馏方法兼顾效率，可零样本泛化到未知环境。
---

## 摘要
自主无人机导航通常由将感知、建图和规划分离为不同阶段的流水线解决，这会传播误差、累积延迟，并需要针对特定环境重新调整。端到端生成模型通过将原始观测直接映射到轨迹来消除这些接口，但继承了一个微妙的故障模式：在干净数据上训练后，它们无法识别观测何时不可靠，并将玻璃、镜面、过曝表面等退化区域视为有效的规划证据。我们提出了一种用于3D无人机导航的可靠性感知扩散规划器。它根据观测以及场景级可靠性热图来条件化轨迹生成，该热图标记了感知不可信的区域，由轻量级网络在实时规划预算内蒸馏视觉语言模型的开放词汇推理生成。为了无需重新训练即可泛化到未见环境，我们使用可微分的两阶段ESDF成本引导去噪过程，该成本将来自深度的物理障碍和来自高度不可靠区域的虚拟障碍同等对待。在仿真和真实四旋翼实验中，我们的规划器相比最先进的扩散基线产生了明显更安全的轨迹，将障碍违规率从40.3%降低到9.6%，并将穿越区域的平均可靠性从0.588提高到0.925。仅消融可靠性项就使平均可靠性从0.898下降到0.783，确认了其决定性作用，而蒸馏使框架运行速度比完整视觉语言模型快2倍。

## Abstract
Autonomous UAV navigation is conventionally solved by pipelines that separate perception, mapping, and planning into distinct stages, which propagates errors, accumulates latency, and requires environment-specific retuning. End-to-end generative models remove these interfaces by mapping raw observations directly to trajectories, but inherit a subtle failure mode: trained on clean data, they cannot recognise when an observation is unreliable, and treat degraded regions such as glass, mirrors, and overexposed surfaces as valid evidence for planning. We present a reliability-aware diffusion planner for 3D UAV navigation. It conditions trajectory generation on the observation together with a scene-level reliability heatmap that marks where perception cannot be trusted, produced by a lightweight network that distils the open-vocabulary reasoning of a vision-language model within the real-time planning budget. To generalise to unseen environments without retraining, we steer the denoising process with a differentiable two-stage ESDF cost that treats physical obstacles from depth and virtual obstacles from highly unreliable regions on equal footing. In simulation and on a real quadrotor, our planner produces markedly safer trajectories than a state-of-the-art diffusion baseline, reducing the obstacle-violation rate from 40.3% to 9.6% and raising the mean reliability of traversed regions from 0.588 to 0.925. Ablating the reliability term alone drops mean reliability from 0.898 to 0.783, confirming it as the decisive component, while distillation runs the framework up to 2 times faster than the full vision-language model.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义

**研究动机**：传统无人机自主导航采用感知-建图-规划的模块化流水线，存在误差传播、延迟累积、需针对环境重新调整等问题。端到端生成模型虽然能直接将原始观测映射到轨迹，但存在一个关键故障模式：模型在干净数据上训练，无法识别观测是否可靠，会将玻璃、镜面、过曝等退化感知区域当作有效规划证据，导致危险轨迹。

**整体含义**：本文提出了一种**可靠性感知的扩散规划器**，通过将感知可靠性作为轨迹生成的显式条件，解决了端到端模型在不可靠感知区域失效的问题，显著提升了无人机在室内非结构化环境中的安全导航能力。

## 2. 方法论

### 核心思想
将场景级可靠性热图作为扩散模型的条件输入，并通过蒸馏视觉-语言模型（VLM）获得轻量级实时可靠性估计；同时使用可微分两阶段ESDF成本引导去噪过程，将物理障碍和虚拟障碍（来自高度不可靠区域）同等对待，实现无需重新训练的零样本泛化。

### 关键技术细节
- **可靠性估计**：使用AnyTraverse（VLM）生成可靠性热图\( M \in [0,1]^{H \times W} \)，对玻璃、过曝、反射等概念赋权（权重均为1.0）。训练时蒸馏到轻量级学生网络MiT-B0，使用冻结的ViT-Adapter/DINOv2作为教师特征监督，目标函数包含特征模仿损失、加权L1回归损失、SSIM结构损失。
- **扩散轨迹规划器**：采用DDPM框架，1D U-Net作为去噪器。状态表示对比了绝对ego-frame航点和相对位移（delta）。条件包括初始速度、目标点、图像特征（来自学生编码器或VLM）、可靠性热图块。辅助损失含平滑、锚定原点、速度一致性和可靠性惩罚。
- **可训练自由引导**：在推理时用成本函数梯度修正去噪得分。成本函数包含：
  - 障碍成本：基于3D ESDF的CHOMP成本。
  - 目标成本：最终航点与目标距离。
  - 可靠性成本：分两阶段——低可靠性像素（\( M \leq 0.3 \)）作为硬障碍融入ESDF；中等可靠性（\( 0.3 < M \leq 0.6 \)）作为弱CHOMP成本，作用距离更大。

### 公式/算法流程
- 正向扩散：\( x_t = \sqrt{\bar{\alpha}_t} x_0 + \sqrt{1-\bar{\alpha}_t} \epsilon \)
- 训练损失：\( L_{\text{diff}} = \mathbb{E}[\|\epsilon - \epsilon_\theta(x_t, t, c)\|^2] \)
- 引导采样：\( \tilde{\epsilon}_\theta = \epsilon_\theta - s \nabla_{x_t} C \)，其中\( C = w_{\text{obs}}C_{\text{obs}} + w_{\text{goal}}C_{\text{goal}} + w_{\text{perc}}C_{\text{perc}} \)

## 3. 实验设计

### 数据集/场景
- **合成数据集**：在Pegasus Simulator中模拟仓库环境，通过立体深度重建、可靠性估计、几何路径规划、物理模拟生成配对数据（观测、可靠性热图、目标轨迹）。
- **真实世界部署**：8英寸四旋翼（ArduPilot+MAVROS），Intel RealSense D455深度相机，在真实室内场景测试。

### 对比方法
- **NoMaD**：基于视觉条件的扩散基线，无可靠性表征。
- 自身消融：不同轨迹表示（ego-path vs delta-path）、有无可靠性条件、有无ESDF引导，共4种组合。

### 评测指标
- **路径效率**：直线距离/实际路径长。
- **安全性**：障碍违规率（碰撞航点比例）、最小障碍距离、穿越区域平均可靠性。

## 4. 资源与算力

论文未明确报告训练时长、GPU数量等训练算力。推理阶段：
- GPU：单张NVIDIA RTX 4070，内存占用约500 MB。
- 蒸馏后框架运行速度比完整VLM快2倍，可满足实时规划预算。

## 5. 实验数量与充分性

- **消融实验**：对比了两种轨迹表示在有无引导下的路径效率（4组），以及有无可靠性、有无引导的完整安全性对比（8组条件）。
- **对比实验**：与NoMaD在4项安全性指标上比较。
- **真实世界测试**：提及在真实四旋翼上部署，但未给出具体定量数值（仅有仿真和真实部署的定性说明）。
- **充分性评价**：消融设计全面，分离了可靠性条件和引导的贡献；但真实实验数据偏少，且未报告多次重复的统计结果（仅仿真给出均值±标准差）。整体上实验较充分，但真实性验证略显不足。

## 6. 主要结论与发现

1. 可靠性感知显著提升安全性：相比NoMaD，障碍违规率从40.3%降至9.6%，最小障碍距离从0.009m提升至0.562m，平均可靠性从0.588提升至0.925。
2. 可靠性条件的作用关键：消融可靠性引导后，平均可靠性从0.898降至0.783，确认其为决定性因素。
3. 轨迹表示选择：ego-path表示优于delta-path，在引导下仍能高效接近目标，而delta-path累积误差导致偏离。
4. 蒸馏有效：学生网络达到教师VLM的可靠性估计性能，且速度快2倍，满足实时性。
5. 零样本泛化可行：训练自由引导无需重新训练即可适应未知场景。

## 7. 优点

- **创新性**：首次将感知可靠性作为扩散模型的显式条件，打破端到端模型对“完美感知”的隐含假设。
- **实用性**：通过知识蒸馏将大模型推理能力压缩到轻量网络，兼顾效果与实时性。
- **可迁移性**：训练自由引导机制无需环境特定调整，支持零样本泛化。
- **实验设计**：消融实验系统分解了各个组件的贡献，验证了可靠性条件的关键作用。

## 8. 不足与局限

- **热图精度限制**：可靠性热图受限于教师模型AnyTraverse和蒸馏误差，停留在场景级而非像素校准，可能漏检或误判。
- **计算成本**：每步引导需查询ESDF，随去噪步数和航点数量线性增长，在更受限嵌入式硬件上可能难以达到高重规划频率。
- **评估覆盖**：真实实验仅给出定性描述，缺乏定量对比数据（如真实场景的故障率、可靠性指标），且未测试动态环境、户外光照或对抗性场景。
- **可推广性**：仅评估室内仓库和真实室内场景，未涉及户外或跨本体（如地面机器人）的迁移验证。

（完）
