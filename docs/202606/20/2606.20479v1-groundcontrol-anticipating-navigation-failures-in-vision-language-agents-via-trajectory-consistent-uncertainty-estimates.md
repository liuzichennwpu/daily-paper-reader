---
title: "GroundControl: Anticipating Navigation Failures in Vision-Language Agents via Trajectory-Consistent Uncertainty Estimates"
title_zh: GroundControl：通过轨迹一致的不确定性估计预测视觉-语言智能体中的导航故障
authors: "Nastaran Darabi, Divake Kumar, Sina Tayebati, Devashri Naik, Amit Ranjan Trivedi"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20479v1"
tags: ["query:uav-vln"]
score: 9.0
evidence: 视觉语言导航代理的失败预测
tldr: 视觉语言导航代理在基准任务中平均成功率尚可，但常因轨迹级故障（如振荡、停滞、绕路）而失败。本文提出GroundControl，一种轨迹一致的不确定性估计方法，通过卡尔曼滤波器建模距离演化，结合归一化创新统计与轨迹特征，捕捉导航行为中的几何与时间不一致性。在5个EB-Navigation场景上，GroundControl在成功率和SPL选择性评估中均显著优于熵、共形及启发式基线，实现近乎完美的排序性能。该方法为可靠部署提供了可解释的故障预测信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20479v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1806, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2606.20479v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 892, \"height\": 1155}, {\"url\": \"assets/figures/arxiv/2606.20479v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1808, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.20479v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1805, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2606.20479v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1841, \"height\": 1157}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20479v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 236}, {\"url\": \"assets/tables/arxiv/2606.20479v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1833, \"height\": 443}, {\"url\": \"assets/tables/arxiv/2606.20479v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1829, \"height\": 442}]"
motivation: 现有不确定性信号仅反映瞬时动作熵，无法预测轨迹级故障（振荡、停滞等），需在执行过程中提前捕捉失败动态。
method: GroundControl基于恒定速度卡尔曼滤波器建模距离演化，结合归一化创新统计及进度、单调性、路径效率、振荡行为等轨迹特征，计算几何与时间不一致性分数。
result: 在5个EB-Navigation场景（N=300）上，GPT-4o模型成功选择性评估加权平均E-AURC为0.0024，显著优于熵、共形及启发式基线；SPL选择性评估中AURC和E-AURC均为最低。
conclusion: 偏离目标导向动力学的建模提供了可解释且鲁棒的不确定性信号，能有效预测视觉语言导航代理的失败行为。
---

## 摘要
视觉-语言导航智能体在基准任务上获得了有竞争力的平均成功率，但故障通常以可预测的轨迹层面崩溃出现，例如振荡、停滞或低效绕路。因此，可靠部署需要能够在执行过程中预测新兴故障动态的不确定性信号，而不仅仅是反映瞬时动作熵。我们提出了GroundControl，这是一种轨迹一致的不确定性估计器，定义为在整个回合中聚合的与名义目标导向的到目标距离动态的统计偏差。GroundControl使用恒定速度卡尔曼滤波器对距离演化进行建模，并将归一化创新统计与补充轨迹特征相结合，这些特征捕获进度、单调性、路径效率和振荡行为。所得的不确定性分数反映了导航行为中的几何和时间不一致性，而不是局部预测分散性。为了独立于任务成功评估不确定性质量，我们形式化了选择性风险覆盖导航（SRCN），这是一种使用风险-覆盖曲线和AURC/E-AURC摘要来衡量不确定性分数按故障或低效对回合排序有效性的协议。在五个EB-Navigation分割（N=300回合）上，轨迹一致的不确定性在基于成功的选择性风险下实现了近乎oracle的排序，GPT-4o模型的加权平均E-AURC_{SR}=0.0024，显著优于基于熵、共形和启发式基线。在基于SPL的选择性评估下，GroundControl在所有模型和导航分割中始终达到最低的AURC和E-AURC。这些结果表明，对目标导向动态的偏差建模为预测视觉-语言智能体中的导航故障提供了可解释且鲁棒的信号。

## Abstract
Vision-language navigation agents achieve competitive average success on benchmark tasks, yet failures often arise through predictable trajectory-level breakdowns such as oscillation, stagnation, or inefficient detours. Reliable deployment, therefore, requires uncertainty signals that anticipate emerging failure dynamics during execution rather than reflect only instantaneous action entropy. We introduce \emph{GroundControl}, a trajectory-consistent uncertainty estimator defined as statistical deviation from nominal goal-directed distance-to-goal dynamics aggregated over an episode. GroundControl models distance evolution using a constant-velocity Kalman filter and combines normalized innovation statistics with complementary trajectory features capturing progress, monotonicity, path efficiency, and oscillatory behavior. The resulting uncertainty score reflects geometric and temporal inconsistency in navigation behavior rather than local prediction dispersion. To evaluate uncertainty quality independently of task success, we formalize \emph{Selective Risk--Coverage Navigation (SRCN)}, a protocol that measures how effectively an uncertainty score ranks episodes by failure or inefficiency using risk--coverage curves and AURC / E-AURC summaries. Across five EB-Navigation splits ($N=300$ episodes), trajectory-consistent uncertainty achieves near-oracle ordering under success-based selective risk, with weighted-average $\mathrm{E\text{-}AURC}_{\mathrm{SR}}=0.0024$ for the GPT-4o model, substantially outperforming entropy-, conformal-, and heuristic baselines. Under SPL-based selective evaluation, GroundControl consistently achieves the lowest AURC and E-AURC across models and navigation splits. These results show that modeling deviation from goal-directed dynamics provides an interpretable and robust signal for anticipating navigation failures in vision-language agents.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言导航（VLN）智能体在基准任务中平均成功率尚可，但在实际部署中，故障通常以轨迹级别的典型崩溃形式出现，如**振荡、停滞、低效绕路**等，这些是渐进发生的，而非瞬时决策错误。
- **现有方法不足**：目前VLN系统缺乏在执行过程中检测此类失败动态的机制。现有的不确定性信号（如预测熵、token级置信度）仅捕捉**瞬时动作层面的模糊性**，无法反映轨迹是否与朝向目标的几何进展保持一致。一个智能体可能维持高步骤置信度，同时执行导致振荡或低效绕路的动作。
- **研究动机**：需要一种能反映**轨迹级目标导向动态一致性**的不确定性信号，以在故障发生前进行预判，实现可靠部署。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将不确定性估计问题转化为检测与名义目标导向运动（即距离-目标动态）的**统计显著偏差**。成功的导航回合中，距离-目标信号遵循**结构化演化**（持续进展，波动有界）；系统性的违背（振荡、停滞、发散、低路径效率）提供偏差证据。
- **关键技术细节**：
    - **恒定速度卡尔曼滤波器（KF）建模**：将距离-目标信号 `d_t` 视为状态 `x_t = [d_t, ḋ_t]^T`，采用线性高斯动态模型（`F`、`H`、过程噪声 `Q`、观测噪声 `σ²`）。滤波器预测名义目标导向进展。
    - **统计偏差指标**：
        - **归一化创新统计量** `ν²_t / S_t`（`ν_t` 是测量值与一步预测值之差，`S_t` 是创新协方差），遵循χ²分布，作为一致性统计检验。
        - **后验协方差生长** `log det(Σ_{t|t})`，反映滤波器对局部距离动态的不确定性。
    - **每步不确定性分数** `H_t`：融合三部分：
        1. 后验信念分散 `log det(Σ_{t|t})`
        2. 动作层熵 `H(α_t)`（基于执行动作的softmax概率）
        3. 归一化创新平方 `ν²_t / S_t`
        固定权重（λ1=1.0, λ2=0.5, λ3=0.8）。
    - **回合级分数**：对每步分数取最大值后经sigmoid映射，再与四个互补的轨迹特征（进度、单调性、路径效率、振荡行为）线性融合。权重固定（w_skf=0.30, w_prog=0.25, w_mono=0.20, w_eff=0.15, w_osc=0.10）。
    - **主动恢复**：可触发的切换控制器，基于运行时不确定性分数阈值选择不同策略（正常、恢复、暂停），但本文主要侧重于评估。

## 3. 实验设计：数据集、基准、对比方法

- **数据集/场景**：使用 **EB-Navigation** 基准，包含五个截然不同的分割（子集）：
    - `base`，`common sense`，`complex instruction`，`visual appearance`，`long horizon`
    - 每个分割 60 个回合，共 **300 个回合**。
    - 导航动作空间 |A|=8（前进、后退、左移、右移、左转、右转、向上看、向下看）。
- **评估基准（SRCN协议）**：提出 **Selective Risk–Coverage Navigation (SRCN)**，是一种用于评估回合级不确定性排序质量的协议。通过**风险-覆盖曲线**、**AURC**（曲线下面积）和 **E-AURC**（相对于oracle排序的均匀风险）来衡量排名质量。有两种损失：
    - 成功损失 `ℓ_{SR}=1-Succ_i`
    - SPL损失 `ℓ_{SPL}=1-SPL_i`
- **对比方法**（7种基线）：
    - **Oracle**：按真实损失排序，作为理论最优下界。
    - **Conformal (ACI)**：自适应共形推断，基于外推残差。
    - **Predictive Entropy**：动作直方图的归一化香农熵。
    - **Self-Consistency**：计划不稳定性的Jaccard相似度。
    - **Invalid-action rate**：环境拒绝动作的比率。
    - **Random**：均匀随机分数，作为下界。
- **基础VLN模型**：使用三种不同的视觉语言模型作为导航策略：
    - **GPT-4o**，**GPT-5-mini**，**Gemini-1.5-Flash**

## 4. 资源与算力

- **明确说明**：文中**未明确说明**所使用的GPU型号、数量、训练时长等计算资源。
- **推测**：由于方法本质上是一个运行在预训练模型之上的**轻量级后处理估计器**（卡尔曼滤波 + 特征融合），主要计算开销可能来自VLM推理。随着VLM本身的计算代价由部分工作承担，添加GroundControl的计算成本相对较低。

## 5. 实验数量与充分性

- **数量**：
    - **主要实验**：在5个分割 × 3个基础模型上，比较GroundControl与6种有意义的基线（共7种方法），生成两个表格（表II和表III）。
    - **诊断图**：
        - 图2：不确定性分数条件分布（成功/失败）。
        - 图3 & 4：风险-覆盖曲线（基于成功和SPL）。
        - 图5：不确定性 vs. SPL的散点图及皮尔逊相关性。
    - **性能数字**：表I给出基础模型的标准导航性能（成功率、SPL）。
- **充分性与公平性**：
    - **充分**：使用多个不同模型和多样化的导航分割，能测试方法的泛化能力。SRCN协议提供独立的评估框架。
    - **公平**：GroundControl和所有基线均在**同一固定轨迹集**上评估，不修改底层策略。权重固定，未针对特定基线优化，避免参数调优偏差。与oracle下界对比，能清晰展示与理论最优的差距。
    - **局限**：
        - 未提及**消融实验**验证GroundControl各组件（如KF信号、四种轨迹特征）的贡献。
        - 仅使用**单一基准数据集**（EB-Navigation），环境多样性受限。
        - **未与现有更复杂的VLN失败预测或不确定性估计方法**（如基于贝叶斯批评者的方法）进行对比。

## 6. 论文的主要结论与发现

- **GroundControl在SRCN中的卓越性能**：
    - **成功导向评估**：在五个EB-Navigation分割上实现**接近oracle的排序**。GPT-4o模型的加权平均E-AURC_SR为0.0024，显著优于所有基线。在除`long horizon`外的四个分割上，E-AURC达到0.00。
    - **SPL导向评估**：在所有模型和分割中，持续实现**最低的AURC和E-AURC**。GPT-4o模型平均AURC_SPL 0.29，E-AURC_SPL 0.01。
- **相较于基线的显著优势**：基于熵、共形和启发式的方法在长视距等挑战性场景中表现接近随机，而GroundControl得益于几何进展动态信号，始终保持鲁棒。
- **核心发现**：对目标导向动态的偏差建模提供了**可解释且鲁棒**的不确定性信号，能有效预测VLN智能体的故障。几何进展动态，而非瞬时动作分散，是在更深时间步长下检测导航故障的主要信号。

## 7. 优点

1.  **动机明确、视角新颖**：将不确定性定义为**轨迹级几何与时间一致性**，而非局部决策模糊，直击VLN可靠部署的痛点。
2.  **理论支撑可解释**：基于线性高斯系统假设，利用卡尔曼滤波的归一化创新统计量（Mahalanobis残差）作为统计检验，明确衡量偏离名义目标导向运动的程度。
3.  **轻量且无需训练**：仅依赖固定权重的卡尔曼滤波和特征融合，无需对底层导航模型进行任何微调或训练，计算成本低，易于集成。
4.  **评估协议独立且标准化**：提出的SRCN协议将不确定性评估与任务成功率解耦，通过风险-覆盖曲线及AURC/E-AURC指标，提供了公平、定量、可复用的基准。
5.  **实验设计公平**：所有方法在相同固定轨迹上评估，未调整参数，对比基线全面，涵盖多种代表性信号。
6.  **结果强劲显著**：在多个不同模型和不同难度的场景下，始终以较大优势优于所有基线，近乎达到Oracle排序的性能。

## 8. 不足与局限

1.  **场景与多样性局限**：仅在 **EB-Navigation** 单一基准（共300回合）上评估，场景多样性不足。未在R2R、Habitat等更广泛、更复杂的VLN基准上进行验证，限制了结论的泛化性。
2.  **缺乏消融分析**：未进行消融实验来验证GroundControl各组件（KF统计量 vs. 单个轨迹特征 vs. 融合信号）的相对重要性，使方法设计受到一定支持因素的减弱。
3.  **模型假设的局限**：恒定速度卡尔曼滤波假设**线性高斯动态**，对于复杂室内环境中更复杂的机动机动（如规避障碍物）可能不准确，但文中认为它作为名义代理是合理的。
4.  **未考虑动态环境**：实验环境可能是静态的，未考虑动态障碍物、人群干扰或环境变化，这些现实因素会显著影响导航动态和不确定性。
5.  **与复杂方法的对比缺失**：未与现有更先进的贝叶斯深度学习方法（如深度集成、贝叶斯批评者）进行对比，这些方法可能提供更丰富的轨迹级不确定性估计。
6.  **主动恢复策略未有效评估**：虽然介绍了主动恢复的切换控制器，但未进行实验验证其有效性或分析其性能，仅作为概念提出。
7.  **权重敏感性与校准问题**：固定权重虽保证了可比性，但未讨论或实验验证结果对不同权重的敏感性。此外，虽然报告了校准相关（ECE）指标，但未与基线进行系统对比或深入分析其校准质量。

（完）
