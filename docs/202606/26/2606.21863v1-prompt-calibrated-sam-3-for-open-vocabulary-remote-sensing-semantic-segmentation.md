---
title: Prompt-Calibrated SAM 3 for Open-Vocabulary Remote Sensing Semantic Segmentation
title_zh: 用于开放词汇遥感语义分割的提示校准SAM 3
authors: "Yanghui Song, Nanqing Liu, Haonan Yin, Yingjie Gao, Chengfu Yang, Qi Ming"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21863v1"
tags: ["query:rs-fusion"]
score: 9.0
evidence: 基于视觉语言模型SAM3的遥感开放词汇语义分割
tldr: "开放词汇语义分割在遥感中受固定标签空间限制，现有SAM3方法存在单提示语义覆盖不足、多提示编码冗余和噪声聚合问题。为此提出ProC-SAM3，通过离线提示池构建多粒度提示并缓存文本嵌入，采用存在引导残差融合与峰值保留聚合抑制噪声。在8个基准上平均mIoU达56.1%，超越无训练方法3.9个百分点，显著提升遥感开放词汇分割性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21863v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1854, \"height\": 975}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 923, \"height\": 729}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 426, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2606.21863v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 426, \"height\": 246}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21863v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1681, \"height\": 536}, {\"url\": \"assets/tables/arxiv/2606.21863v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 602, \"height\": 194}, {\"url\": \"assets/tables/arxiv/2606.21863v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 836, \"height\": 260}]"
motivation: 现有SAM3方法在遥感开放词汇分割中因单提示语义覆盖不足、多提示编码冗余和噪声聚合，导致分割效果受限。
method: 离线构建提示池并缓存文本嵌入，通过存在引导残差融合和峰值保留类聚合，实现高效无噪声的开放词汇分割。
result: "在8个遥感基准上平均mIoU 56.1%，超过之前最佳无训练方法3.9个百分点。"
conclusion: ProC-SAM3通过提示校准和残差融合有效提升遥感开放词汇分割精度，且无需重新训练。
---

## 摘要
开放词汇语义分割（OVSS）在遥感图像中旨在分割固定标签空间之外的类别。最近的基于SAM 3的方法提供了一个有前途的无训练基础，但仍存在三个关键问题：（1）单个类名提示缺乏对复杂遥感类别足够的语义覆盖；（2）将每个类别扩展为多个提示引入了冗余的在线文本编码；（3）直接聚合多个提示响应会将噪声激活传播到最终预测中。为了解决这些问题，我们提出了ProC-SAM3，它从三个互补方面校准SAM 3的提示接口用于遥感OVSS。首先，我们构建了一个离线提示池，其中类别匹配器将MLLM生成的候选者分组为每个类别集合，并且扩展约束使用类别先验知识进一步细化每个集合。其次，得到的文本嵌入被缓存并在所有测试图像中重复使用，消除了重复的文本编码。第三，我们引入了存在引导残差融合，通过提示存在性和置信度来门控不可靠的解码器输出，随后进行峰值保留类聚合，保留小和稀疏对象的细粒度激活。在八个基准上的实验表明，ProC-SAM3达到了平均mIoU 56.1%，比之前最佳的无训练方法高出3.9个百分点。代码将在https://github.com/YanghuiSong/ProC-SAM3上提供。

## Abstract
Open-vocabulary semantic segmentation (OVSS) in remote sensing images aims to segment categories beyond a fixed label space. Recent SAM 3-based methods provide a promising training-free foundation, yet three key issues remain: (1) a single class-name prompt lacks sufficient semantic coverage for complex remote sensing categories; (2) expanding each category into multiple prompts introduces redundant online text encoding; and (3) directly aggregating multiple prompt responses propagates noisy activations into the final prediction. To address these issues, we propose ProC-SAM3, which calibrates SAM 3's prompt interface for remote sensing OVSS from three complementary aspects. First, we construct an offline prompt pool where a Category Matcher groups MLLM-generated candidates into per-category sets, and Expansion Constraints further refine each set using category-specific prior knowledge. Second, the resulting text embeddings are cached and reused across all test images, eliminating repeated text encoding. Third, we introduce Presence-Guided Residual Fusion to gate unreliable decoder outputs by prompt presence and confidence, followed by peak-preserving class aggregation that retains fine-grained activations for small and sparse objects. Experiments on eight benchmarks show that ProC-SAM3 achieves an average mIoU of 56.1%, outperforming the previous best training-free method by 3.9 percentage points. Code will be available at https://github.com/YanghuiSong/ProC-SAM3.

---

## 论文详细总结（自动生成）

# 论文《Prompt-Calibrated SAM 3 for Open-Vocabulary Remote Sensing Semantic Segmentation》详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究问题**：遥感图像开放词汇语义分割（OVSS）旨在分割超出固定标签空间的目标。现有基于SAM 3的无训练方法虽表现出色，但存在三个关键瓶颈：
  1. 单一类名提示（class-name prompt）对复杂遥感类别（如“道路”可能包含沥青路、土路等不同形态）缺乏足够的语义覆盖。
  2. 简单地将每个类别扩展为多个提示，会导致在线重复文本编码，显著增加推理开销。
  3. 直接对多提示响应进行平均或简单聚合，会将噪声激活（如错误预测的高置信度区域）传播到最终预测中。
- **整体含义**：需要一种无训练、无标注的框架，能够从**提示构建、编码效率、掩码聚合**三个层面校准SAM 3的提示接口，从而实现更准确、高效的遥感开放词汇分割。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：ProC-SAM3 通过三个阶段改进SAM 3的提示接口，无需任何训练或真实标注：
  - **(a) 离线提示池构建（Offline Prompt-pool Construction, OPC）**：利用多模态大语言模型（Qwen3-VL）为每个基础类别生成多样化候选描述，然后通过类别匹配器（Category Matcher）将候选词映射到正确的类别，再通过扩展约束（Expansion Constraints，如属性、描述）进一步过滤，形成语义边界清晰、紧凑的提示池。例如，“car”“small vehicle”“large vehicle”被映射到“vehicle”类；“forest”“canopy”“tree”映射到“trees”类。
  - **(b) 提示优化批量推理（Prompt-Optimized Batched Inference, POBI）**：将离线构建的全局提示池编码一次，缓存文本嵌入（text embeddings），在后续所有测试图像中重复使用，避免了在线推理时重复编码。推理时，图像仅通过一次视觉编码器，然后分块批量解码多个提示，提高吞吐量。
  - **(c) 存在引导残差融合（Presence-Guided Residual Fusion, PGRF）**：对每个提示的语义掩码和实例掩码，利用存在分数（presence score）、置信度、分支间一致性（agreement）计算门控因子，仅当提示确实存在且两分支对齐时，才将实例残差融入语义响应；否则退化为仅使用语义响应。最后通过逐像素最大池化（max-pooling）聚合同一类别的多个提示响应，保留小目标激活。
- **关键公式与流程**（文字说明）：
  - 提示池构建：`P_i = Projection(CM(Qwen3(c_i, EC_i)), Attribute_i, Description_i)`
  - 文本嵌入缓存：`T_{Q} = TextEncoder(P*)`
  - 批量解码：`Z_q = MaskDecoder(F, T_q)`
  - 存在引导残差融合：`A_q = 1 - |S^sem_q - S^base_q|`，`Δ_q = [S^base_q - S^sem_q]_+`，`G_q = ρ_q · k_q · A_q`，最终`Ŝ_q = ρ_q · (S^sem_q + G_q ⊙ Δ_q)`
  - 类别聚合：`Ŝ_i(u) = max_{q: π(q)=i} Ŝ_q(u)`，最终`Ŷ(u) = argmax_i Ŝ_i(u)`

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：8个遥感分割基准：Vaihingen、Potsdam、LoveDA、OpenEarthMap (OEM)、iSAID、UAVid、UDD5、VDD。
- **基准（benchmark）**：开放词汇语义分割设定，所有方法均使用SegEarth-OV3定义的类别集合进行评估。
- **对比方法**：包括VIP、SCLIP、ClearCLIP、ProxyCLIP、SegEarth-OV、RSCLIP、CorrCLIP、ConInfer、ReAttnCLIP、ActiveSAM、SegEarth-OV3等。其中SegEarth-OV3是当前最先进的无训练方法，也是ProC-SAM3的主要对比对象。
- **实验数量**：
  - 主表（Table I）报告了所有方法在8个数据集上的mIoU，共十几组对比。
  - 消融实验（Table II）在3个数据集（UDD5、OEM、LoveDA）上评估OPC和PGRF的贡献。
  - 融合策略对比（Fig. 4）在3个数据集上比较Max、Mean和PGRF。
  - 推理速度对比（Fig. 5）在4个数据集上比较有无POBI。
  - 提示构建策略消融（Table III）在3个数据集上比较5种策略（类名、开放MLLM、纯文本扩展、图像条件扩展、约束引导提示池）。

## 4. 资源与算力
- **文中明确说明**：所有实验在一块RTX 4090 GPU（24GB）上运行，无需训练。Qwen3-VL只在离线构建提示池时使用，随后从GPU内存中卸载，避免与SAM 3推理争抢显存。图像编码器使用半精度（BF16），解码器保持全精度（FP32）。未提及具体推理时长或批处理大小细节。

## 5. 实验数量与充分性
- **实验数量**：总计超过5组不同角度的实验（主表+4组消融/对比），覆盖8个数据集，消融实验涉及3-4个代表性数据集。
- **充分性判断**：
  - **积极方面**：对比了10多种方法，包含不同骨干（CLIP、DINOv3、SAM3等），消融了每个组件（OPC、PGRF、POBI）以及提示构建策略，实验设计较为全面。
  - **不足**：消融实验仅选取了3个数据集（UDD5、OEM、LoveDA），未在全部8个数据集上进行消融，可能存在数据集偏差。此外，仅报告了mIoU单一指标，未提供F1、IoU per class等细粒度分析。代码未附链接（将提供），但无法验证复现性。
- **客观公平性**：对比方法的结果多数来源于原论文或作者复现（带*标记），并统一使用SegEarth-OV3的类别定义，确保公平。但部分方法可能未在所有数据集上报到，平均计算时可能存在不完全对齐。

## 6. 论文的主要结论与发现
- ProC-SAM3在8个数据集上平均mIoU达到56.1%，超过之前最佳无训练方法SegEarth-OV3（52.2%）3.9个百分点，在所有数据集上均取得最高分。
- 主要增益来自离线提示池构建（OPC），平均贡献+3.7%；存在引导残差融合（PGRF）进一步提升约1.0%。
- 提示优化批量推理（POBI）能显著加速推理，在Potsdam上从1.35 FPS提升至2.42 FPS。
- 无约束的MLLM扩展会导致性能下降，而约束引导的提示池结合图像上下文和类别先验知识最有效。
- 该方法无需任何训练或真实标注，具有实际部署潜力。

## 7. 优点：方法或实验设计上的亮点
- **方法论亮点**：
  - **完全无训练、无标注**：不需要任何下游训练数据或GT掩码，适合遥感标注稀缺场景。
  - **三阶段系统化校准**：从提示语义、编码效率、聚合稳定性三个角度系统解决SAM 3的提示问题，思路清晰。
  - **创新性门控机制**：PGRF利用SAM 3自身提供的存在分数、置信度、分支一致性动态控制实例信息融合，直观且有效。
  - **离线缓存与批量解码**：POBI在几乎无性能损失下显著提速，且不依赖任何额外硬件。
- **实验设计亮点**：
  - 在8个多样化遥感数据集上评估，涵盖城市、农田、无人机等场景，泛化性较强。
  - 消融实验全面，分别验证了三个主要组件的贡献。
  - 对比了多种提示构建策略（从简单类名到MLLM扩展再到约束池），揭示不同策略的影响。
  - 提供了推理速度对比，证明效率提升。

## 8. 不足与局限
- **实验覆盖**：
  - 消融实验仅在3个数据集上进行，未覆盖所有8个数据集，可能遗漏某些数据集上的特殊行为。
  - 仅报告mIoU，缺乏对每个类别IoU、边界精度、小目标性能等更细致分析。
  - 未与需要训练的方法（如少样本或全监督方法）对比，无法体现该方法的上限。
- **偏差风险**：MLLM（Qwen3-VL）生成的候选提示可能包含对特定场景的偏好，且约束定义依赖人工先验，可能引入主观偏差。
- **应用限制**：
  - 提示池构建仍然需要每数据集/类别的前期设计（约束文件），对于全新未知类别无法零成本适应。
  - 依赖SAM 3的预训练，若SAM 3对某些遥感物体（如极细小目标）处理不佳，ProC-SAM3可能也受限于此。
  - 推理速度虽提升，但SAM 3本身较慢，在实际大范围遥感应用中仍有挑战（例如在1块RTX 4090上FPS约1-2.4）。
- **未提及**：未给出不同网络复杂度或不同分辨率下的性能对比；未讨论类别不平衡问题。

（完）
