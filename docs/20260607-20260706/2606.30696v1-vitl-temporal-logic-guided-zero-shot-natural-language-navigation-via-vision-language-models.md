---
title: "ViTL: Temporal Logic-Guided Zero-Shot Natural Language Navigation via Vision-Language Models"
title_zh: "ViTL: 通过视觉语言模型的时间逻辑引导零样本自然语言导航"
authors: "Kaier Liang, Hengde Dai, Cristian-Ioan Vasile"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30696v1"
tags: ["query:uav-vln"]
score: 8.0
evidence: 零样本自然语言导航框架，可直接用于无人机自然语言指令导航
tldr: 面向未知环境的机器人零样本导航，难以处理“先清洁椅子，再打开电视”等含时序逻辑的长时指令；ViTL框架先利用大语言模型将自然语言指令编译为线性时序逻辑公式，再转换为确定性有限自动机来驱动多通道价值图与动态重规划；同时，在视觉语言模型中引入方向分数，为每个前沿方向打分，而非生成方向无关的值；在HM3D数据集上，ViTL成功完成零样本长时时序导航任务，并使单目标导航的准确率和效率优于基线方法，为零样本复杂指令导航提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30696v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1659, \"height\": 1017}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 776, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 376}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30696v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 379}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 282}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 906, \"height\": 300}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 904, \"height\": 336}]"
motivation: 解决现有零样本导航方法仅限于单目标任务，无法理解“先清洁椅子或沙发，然后打开电视”等具有时序逻辑约束的复杂自然语言指令的问题。
method: 利用大语言模型将指令编译为线性时序逻辑，并转换为确定性自动机驱动多通道价值地图与动态重规划，同时引入方向分数机制从视觉语言模型提取方向感知价值。
result: 在HM3D数据集上，ViTL首次实现零样本长时时序导航任务完成，且单目标导航准确率和效率均优于基线。
conclusion: 提出ViTL框架，通过将时序逻辑与方向分数结合，使零样本自然语言导航能够处理复杂长时指令，性能显著提升。
---

## 摘要
让机器人遵循自然语言命令以完成零样本长期任务仍然具有挑战性。这要求从自然语言命令中提取隐式的时间和逻辑约束，并相应地执行多个子任务。最近的零样本物体导航方法使用视觉语言模型（VLM）在未知环境中引导基于边界的探索，但它们仅限于单目标任务。现实世界的命令，如“清洁椅子或沙发，然后打开电视。”需要按时间约束的顺序导航到多个目标，而现有的零样本系统无法处理。我们提出ViTL，一个在两个层面上解决这一差距的框架。在任务层面，我们使用一个大语言模型（LLM）将自然语言命令编译为线性时序逻辑（LTL）公式，然后将其转换为确定性有限自动机（DFA），该自动机协调多通道值图，并在检测到新物体时触发动态重新规划。在导航层面，我们引入了方向评分：不是在整个视野范围内产生与方向无关的值，而是在观察图像上标记边界方向，并从VLM中提取每个方向的评分。在Habitat-Matterport 3D（HM3D）上的实验表明，完整的框架能够实现带有时间约束的自然语言导航任务的零样本长期完成，并且方向评分提高了单目标导航的准确性和效率，优于基线方法。

## Abstract
Enabling robots to follow natural language commands to complete zero-shot long-horizon tasks remains challenging. It requires extracting implicit temporal and logical constraints from natural language commands and executing multiple sub-tasks accordingly. Recent zero-shot object navigation methods use vision-language models (VLMs) to guide frontier-based exploration in unknown environments, but they are limited to single-target tasks. Real-world commands such as "Clean either the chair or the couch, then turn on the tv." require navigating to multiple targets in a temporally constrained order, which no existing zero-shot system can handle. We present ViTL, a framework that addresses this gap at two levels. At the task level, we use a large language model (LLM) to compile natural language commands into Linear Temporal Logic (LTL) formulas, which are then converted into Deterministic Finite Automata~(DFA) that coordinate multi-channel value maps and trigger dynamic replanning when new objects are detected. At the navigation level, we introduce directional score: rather than producing a direction-agnostic value across the entire field of view, we label frontier directions on the observation image and extract per-direction scores from the VLM. Experiments on Habitat-Matterport 3D (HM3D) show that the full framework enables zero-shot long-horizon completion of natural language navigation tasks with temporal constraints, and that directional score improves single-target navigation accuracy and efficiency over the baseline.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
该论文旨在解决机器人零样本长期导航中的一项关键难题：**如何让机器人在未知环境中，不经过特定训练，便能理解和执行包含复杂时序逻辑约束的自然语言指令**（例如：“先清洁椅子或沙发，然后打开电视”）。

-   **研究背景与动机**：现有的零样本物体导航方法虽已利用视觉语言模型取得进展，但它们被严格限制在单目标任务中，无法处理现实世界中普遍存在的、带有先后顺序、选择（或/且）等时序关系的多目标指令。
-   **核心差距**：目前不存在能够完成此类时序约束**多目标**导航任务的零样本系统。这要求在任务层面进行高层规划，并在导航层面进行精确执行，而现有方法未能将二者有效结合。

### 2. 论文提出的方法论
论文提出了一个名为 **ViTL** 的框架，分别从**任务层**和**导航层**两个层面解决上述问题：

-   **任务层规划器**：
    -   **指令转换**：利用一个大语言模型进行**单次离线调用**，将自然语言指令编译成一个线性时序逻辑（LTL）公式，该公式精确地形式化了指令中的所有时序和逻辑约束。例如，将示例指令编译为 $\diamondsuit ((\text{chair} \lor \text{couch}) \land \diamondsuit \text{tv})$。
    -   **自动机构建与动态选择**：将LTL公式进一步转换为一个**确定性有限自动机**。该自动机的图结构编码了所有有效的子任务执行顺序。在任务执行中，系统根据机器人新发现、已完成和已失败的目标集合，动态计算自动机边上的权重，并使用最短路径搜索算法（公式 (2)）来实时选择当前的最优导航目标。若发现更优路径（如在探索沙发时发现了椅子），可触发即时目标切换。
    -   **多通道值图**：系统为指令中的每个目标类别（如椅子、沙发、电视）维护一个并行的值图通道，用于累积空间知识。切换目标时，新通道已包含历史探索信息，避免了重复探索。

-   **导航层执行器**：
    -   **方向评分**：这是导航层的核心创新。不同于现有方法对整个视野打一个无方向性的分数，ViTL将地图上的**前沿方向标注在RGB图像**上（如A、B、C），然后向视觉语言模型提问“为了找到{目标}，我应选择哪个方向？”。通过计算模型输出各选项标记的softmax概率（公式 (4)），得到每个方向与目标相关性的概率分布。
    -   **平滑值图构建**：将每个前沿方向的评分进行时间融合（用场景相关性加权平均，公式 (7)），并在观察扇面（FOV cone）内通过角度线性插值进行空间平滑（公式 (8)），最终生成一个稠密、平滑的二维价值地图。
    -   **动作执行**：从当前激活目标的值图中，选择分数最高的前沿点作为路标，然后使用一个预训练的深度点目标导航策略将其转化为底层动作。

### 3. 实验设计
-   **数据集与环境**：使用 **Habitat 模拟器**，在 **Habitat-Matterport 3D (HM3D)** 数据集上进行评估。
-   **Benchmark**：
    1.  **单目标导航**：使用HM3D ObjectNav v2验证集，包含20个场景的2000个回合，6种目标类别。
    2.  **多目标任务规划**：构建了一个包含2000个由自然语言指令和对应LTL模板生成的复杂导航任务基准，用于单独评估规划器的性能。
-   **对比方法**：
    -   **导航层对比**：比较了多种最新的零样本物体导航方法，包括 VLFM, InstructNav, WMNav, GAMap 等。
    -   **任务层对比**：将ViTL的规划器与一个直接使用大语言模型在每一步做决策的基线方法（**LLM-planner**）进行对比。

### 4. 资源与算力
-   论文提到使用的**视觉语言模型**为 **LLaVA-NeXT-34B**，并采用了 **8位量化（8-bit quantization）** 技术来加载，这表明该模型可以在有限的计算资源下运行。
-   **目标检测**使用了 **YOLOv7-E6E** 模型。
-   对于任务规划实验中的大语言模型，对比的**LLM-planner** 和ViTL的翻译阶段均使用了 **GPT-5.2**。
-   论文中**未明确提及**具体的训练硬件配置（如GPU型号与数量）、训练时长或推理时的延迟数据。

### 5. 实验数量与充分性
论文进行了**多组、分层**的实验，设计较为**充分和客观**：

-   **导航层实验（1组）**：2000个回合的完整单目标导航任务，与多个领先方法对比，验证了其导航核心的有效性。
-   **任务层实验（主要组）**：为了隔离并评估规划器的性能，设计了**2000个任务**的计算实验。通过调扫描一个**观察概率（pobs）参数**（从0.1到1.0共10个档位），模拟了在不同复杂性环境中探索的结果，并与LLM-planner进行全面对比。这种设计公平地规避了低层导航失效对高层规划评估的干扰。
-   **全系统实验（1组）**：在2个高层筛选出的场景中，对比了完整路径长度。
-   **案例研究（2组）**：通过具体案例定性展示了LLM-planner的失败模式。实验设计具有清晰的逻辑层次，从核心组件到端到端系统，并且通过参数扫描和隔离评估保证了对比的公平性。

### 6. 论文的主要结论与发现
-   **方向评分的有效性**：在单目标导航测试中，ViTL仅使用开源模型LLaVA-NeXT-34B便取得了**54.1%的成功率**和**30.9的SPL**，其性能超越了多个基线方法，证明了方向评分比方向无关的全局评分更有效。
-   **任务规划器的优越性**：基于DFA的规划器在任务完成率上（最高达95.3%，受翻译准确率限制）**显著优于**基于LLM的规划器（最高88.3%），且所需的探索次数**更少**。
-   **零样本长时任务完成**：ViTL作为首个此类框架，成功展示了在没有预训练和地图的情况下，完成具有复杂时序约束的自然语言导航任务的能力。

### 7. 优点
-   **创新的框架设计**：将LLM、LTL/DFA的形式化任务规划与VLM驱动的空间探索清晰解耦又有机结合，设计优雅。
-   **方向评分机制**：为VLM在导航中的应用提供了更细粒度和空间敏感的新视角，有效提升了探索效率。
-   **即时性与鲁棒性**：利用DFA进行动态目标选择和重规划，能够灵活应对环境发现，同时保证了时序约束的严格满足。
-   **资源高效**：通过单次LLM调用解决了任务规划问题，避免了运行时反复查询LLM的高成本和累积错误风险，并且导航层使用了可量化的开源模型。

### 8. 不足与局限
-   **子任务简化**：框架假设每个子任务都简化为“到达一个物体”，未处理更复杂的交互或操作语义。
-   **导航成功率瓶颈**：单目标导航约54%的成功率仍是系统级长时任务（如3个目标）成功的根本瓶颈，导致在随机场景中进行端到端长时任务评估的统计意义有限。
-   **实验规模**：全系统评估仅在两个经过筛选的高成功率场景下进行，虽然隔离了变量，但缺乏在更广泛随机场景中的验证。
-   **对外部模型的依赖**：框架性能很大程度上依赖于VLM的理解能力、目标检测器的准确性和LTL翻译的准确性，这些模块的任何失效都会传播到最终结果。

（完）
