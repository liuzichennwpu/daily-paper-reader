---
title: "DART-VLN: Test-Time Memory Decay and Anti-Loop Regularization for Discrete Vision-Language Navigation"
title_zh: DART-VLN：用于离散视觉语言导航的测试时记忆衰减与防循环正则化
authors: "Shaoheng Zhang, Zhichen Li, Jie Mei"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01043v1"
tags: ["query:uav-vln"]
score: 8.0
evidence: 针对离散视觉语言导航的测试时记忆衰减与防循环正则化框架
tldr: 记忆基离散视觉语言导航（VLN）在测试时易受记忆陈旧和局部回溯影响，导致导航失败。本文提出训练无关的测试时控制框架DART-VLN，结合测试时记忆衰减与抗循环正则化：前者在读取侧重新加权记忆以抑制陈旧冗余证据，后者在动作选择侧惩罚即时反转。在R2R和REVERIE基准上，该方法在多个关键设置下提升导航性能，同时产生更短轨迹和更低运行时间，实现了质量与效率的良好平衡。该框架无额外参数，不改动骨干网络，为提升VLN可靠性与效率提供了轻量级解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01043v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1825, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01043v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1807, \"height\": 532, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01043v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1862, \"height\": 619, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01043v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 582, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01043v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 884, \"height\": 434, \"label\": \"Table\"}]"
motivation: 解决记忆基离散VLN在测试时记忆陈旧与局部回溯导致的失败，提升冻结骨干的可靠性和效率。
method: 提出训练无关的DART-VLN框架，通过测试时记忆衰减重加权抑制陈旧证据，并用抗循环正则化惩罚即时反转。
result: 在R2R和REVERIE上，衰减+抗循环策略实现最佳质量-效率权衡，缩短轨迹并降低运行时间，提升导航性能。
conclusion: 测试时控制框架无需重训练，能有效提升记忆基离散VLN的可靠性与效率。
---

## 摘要
基于记忆的离散视觉语言导航（VLN）智能体必须在部分可观测条件下行动，然而即使是强大的冻结骨干网络在测试时仍显脆弱。两种常见的失效模式是记忆读出时的过时历史证据和动作选择时的低效局部回溯。我们提出 DART-VLN，一种无需训练的、用于离散 VLN 的测试时控制框架。DART-VLN 结合了测试时记忆衰减与防循环正则化：前者是一种读取端记忆重加权规则，在不修改存储内容的情况下抑制过时和冗余证据；后者是一种轻量级的下一跳惩罚，在动作选择时阻止立即反向移动。该框架不引入任何新的可学习参数，且保持已学习的骨干网络不变。在 R2R 和 REVERIE 上的实验显示出一致的模式：仅衰减可提供稳定的读取端增益，而衰减加防循环则实现了最佳的整体质量-效率权衡，在关键设置中生成了更短的轨迹、更低的运行时间以及更优的导航性能。行为分析进一步证实，防循环正则化减少了局部回溯，并在冻结骨干网络下提高了路径效率。总体而言，结果表明适度的测试时控制可以在无需重新训练的情况下使基于记忆的离散 VLN 更加可靠和高效。

## Abstract
Memory-based discrete vision-language navigation (VLN) agents must act under partial observability, yet even strong frozen backbones remain vulnerable at test time. Two common failure modes are stale historical evidence at memory readout and inefficient local backtracking during action selection. We present DART-VLN, a training-free test-time control framework for discrete VLN. DART-VLN combines Test-Time Memory Decay, a read-side memory reweighting rule that suppresses stale and redundant evidence without rewriting stored content, with Anti-Loop Regularization, a lightweight next-hop penalty that discourages immediate reversals during action selection. The framework introduces no new learnable parameters and leaves the learned backbone unchanged. Experiments on R2R and REVERIE show a consistent pattern: decay-only provides stable read-side gains, while decay+anti-loop achieves the best overall quality-efficiency trade-off, yielding shorter trajectories, lower runtime, and improved navigation performance in key settings. Behavioral analysis further confirms that anti-loop regularization reduces local backtracking and improves path efficiency under frozen backbones. Overall, the results show that modest test-time control can make memory-based discrete VLN more reliable and efficient without retraining.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：基于记忆的离散视觉语言导航（VLN）智能体在测试时，即使使用冻结的、强大的骨干网络，仍会出现两种典型失效模式：
    - **记忆读出的过时证据**：历史信息未能随时间衰减，导致噪声积累。
    - **动作选择的低效回溯**：智能体频繁执行即时的反向移动或短循环，浪费步数并增加计算开销。
- **整体含义**：该工作探索在不重新训练模型、不修改骨干网络的前提下，通过轻量级测试时控制来提升记忆基离散VLN的可靠性与效率。其核心洞察是，适度的推理解耦干预（读取端记忆衰减 + 动作端防循环惩罚）即可带来可观的性能与行为改善。

### 2. 论文提出的方法论
- **核心思想**：提出 DART‑VLN，一个完全非训练的测试时控制框架，包含两个互补模块：
    - **测试时记忆衰减（Test-Time Memory Decay）**：在记忆读出时重新加权记忆槽，抑制过时、过度重复或低新颖性的历史证据，但**不改写**已存储的记忆内容。
    - **防循环正则化（Anti-Loop Regularization）**：在候选动作评分上施加一项轻量级惩罚，抑制下一步直接回到上一视点（即时回溯）及多次重复访问的局部节点。
- **关键技术细节**：
    - **记忆衰减（读取端）**：
        - 为每个记忆槽维护年龄 \(a_i\)、访问计数 \(c_i\) 和基于特征余弦相似度的新颖性 \(n_i\)。
        - 读出权重 \(w_i\) 由指数衰减因子、重复抑制因子和新颖性奖励因子共同决定，并裁剪到 \([w_{\min}, w_{\max}]\)。
        - 该权重仅在记忆聚合时作用，存储内容不变。
    - **防循环正则化（动作端）**：
        - 针对当前视点通向每个候选目标视点的**图上一跳** \(h(v)\)，定义惩罚项：若 \(h(v)\) 等于上一视点则施加回溯惩罚 \(\beta_{\text{back}}\)；若 \(h(v)\) 已被重复访问超过 \(k\) 次则额外施加轻微惩罚 \(\beta_{\text{rev}}\)。
        - 最终动作得分 \(s'_t(v) = s_t(v) - p_t(v)\)，再进行贪心选择。
    - **设计原则**：保守的测试时控制，不引入可学习参数，仅干预读取和最终动作评分。
- **算法流程**（文字概括）：
    1. 在每个时间步，编码指令和当前观察。
    2. 利用式(3)计算记忆权重，完成加权记忆聚合，得到候选动作原始得分。
    3. 对每个候选目标计算图下一跳，并利用式(4)计算其惩罚项。
    4. 用式(5)调整动作得分，选择得分最高的动作。
    5. 若选择“停止”则结束，否则更新访问计数和记忆元数据。

### 3. 实验设计
- **数据集与基准**：
    - **R2R**（Room‑to‑Room）：标准离散VLN基准，报告验证集未见环境（val unseen）和测试集未见环境（test unseen）上的指标。
    - **REVERIE**：远程物体指代表达导航基准，因官方测试服务器已不可用，仅报告 val unseen 结果。
- **基线骨干网络**：使用 **GridMM** 作为记忆基离散导航管线，所有变体均基于同一冻结检查点，仅切换推理时的控制开关。
- **对比方法**：
    - 与多个已发表方法对比：VLN‑BERT, AirBERT, SEvol, HOP, HAMT, TD‑STP, DUET, BEVBert 等。
    - 内部消融/边界变体：
        - **update‑only**：仅写端记忆更新。
        - **decay‑only**：仅读端记忆衰减。
        - **full‑mode**：写端更新 + 读端衰减。
        - **decay+anti‑loop**：主推的 DART‑VLN 配置（读端衰减 + 防循环）。

### 4. 资源与算力
- 文中明确提到所有运行时测量均在 **NVIDIA GeForce RTX 5070 Ti GPU** 上，采用相同实现与硬件设置下完成。
- 由于方法完全**无需训练**，故未提及任何训练阶段的 GPU 规格、数量或训练时长；仅报告推理耗时对比。

### 5. 实验数量与充分性
- **实验覆盖**：
    - 在 **R2R** 上提供了 val unseen 和 test unseen 两个分割的完整主表（表 I）。
    - 在 **REVERIE** 上提供了 val unseen 主表（表 II）。
    - 专门的行为诊断表格（表 III）分析了回溯率、平均步数与轨迹长度。
    - 给出了定性路径对比图示（图 2）。
- **消融对比**：系统比较了 **4 种推理变体**（update‑only, decay‑only, full‑mode, decay+anti‑loop）与 GridMM 基线，可清晰分离各模块贡献。
- **公平性与客观性**：
    - 所有变体均使用相同骨干权重，仅推理时控制不同，对比设计干净。
    - 跨论文对比时谨慎指出运行时不可直接比较，仅测量 GridMM 系列在同一环境下的用时，较为客观。
    - 行为分析聚焦于防循环模块的直接效果（回溯率），而非过度归因于全部指标提升。
- **充分性评价**：实验覆盖两个主流离散VLN基准，消融充分，行为诊断与效率分析支持主要结论，整体设计具有较好的内部一致性和可解释性。

### 6. 论文的主要结论与发现
- **一致的趋势**：decay‑only 可稳定提供读取端的性能增益（NE 降低、SR/SPL 保持或微升、运行时间大幅下降）；在此基础上加入防循环正则化（decay+anti‑loop）实现了**最佳的整体质量—效率权衡**：显著缩短轨迹长度，进一步降低运行时间，并提升导航成功率与路径效率（SPL、RGSPL 等）。
- **防循环的直接效应**：行为分析证明，防循环正则化主要作用于降低即时回溯率和平均执行步数，与效率指标的改善方向一致，并不保证所有端点指标的单调增益。
- **方法设计哲学**：在冻结骨干下，保守且范围明确的测试时干预比激进的记忆改写更可靠且有效。

### 7. 优点
- **无需训练，零入侵**：不引入新参数，不更改骨干网络，可直接嵌入已有模型推理流程。
- **轻量且解释性强**：记忆衰减基于可解释的元数据（年龄、频次、新颖性），防循环惩罚作用于图局部行为，效果透明。
- **效率提升突出**：在 R2R 和 REVERIE 上，主推配置的运行时间大幅下降（例如 R2R val unseen 从 937.99s 降至 666.37s，REVERIE 从 4329.67s 降至 1497.98s），同时轨迹更短。
- **完善的消融与边界分析**：通过 decay‑only、update‑only、full‑mode 等变体清晰论证了保守读端控制是更可靠的基座，防循环叠加带来进一步的效率改善。
- **行为验证**：提供了回溯率和平均步数的定量诊断，使效率改进与具体的行为变化直接关联。

### 8. 不足与局限
- **骨干与领域限定**：方法基于 GridMM 风格的离散 VLN 管道和离散导航图，在连续环境或其他类型记忆结构（如拓扑地图、Transformer 隐式记忆）上的泛化性未经验证。
- **任务范围**：仅评估了 R2R 和 REVERIE，REVERIE 的 test unseen 因评估服务器不可用而缺失，可能低估或遗漏泛化性能的某些方面。
- **边界变体不稳定**：写端更激进的 full‑mode 和 update‑only 收益不一致，在某些情况下性能甚至劣于基线，限制了方法向写端扩展的潜力。
- **端点指标的依赖性**：防循环模块的直接贡献主要体现在局部行为与效率，对于一些端点导航成功率的提升可能与其他因素（如更干净的记忆状态）存在混杂，无法单独归因。
- **超参数调优**：虽然方法无需训练，但衰减率、惩罚系数等超参数仍需在验证集上调参，可能存在对特定环境的过拟合风险，文中未充分讨论跨环境的敏感性。
- **无动态/自适应机制**：衰减和惩罚规则是静态启发式，未随任务难度或环境上下文自适应调整，在极端或多样化场景下可能效果下降。

（完）
